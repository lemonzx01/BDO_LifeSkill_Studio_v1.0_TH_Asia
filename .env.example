# Postgres connection (Neon / Vercel Postgres). Leave unset to use the embedded
# PGlite database under .data/pglite for local development.
# DATABASE_URL=postgres://user:password@host/db?sslmode=require
